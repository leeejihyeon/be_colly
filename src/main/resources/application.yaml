server:
  port: 18080

spring:
  application:
    name: colly
  datasource:
    url: jdbc:mysql://localhost:3306/colly?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: colly.auth@gmail.com
    password: "kefb rkul dfrx dnte"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

colly:
  auth:
    mail:
      from: colly.auth@gmail.com
      magic-link-base-url: http://localhost:18080
      app-deep-link-base: collyapp://auth/magic-link

logging:
  level:
    org.hibernate.SQL: debug
